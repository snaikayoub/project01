<form method="post" action="{{ path('app_astreinte_delete', {'id': astreinte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ astreinte.id) }}">
    <button class="btn">Delete</button>
</form>
